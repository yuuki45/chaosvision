{
  "name": "chaos-vision-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies CHAOS VISION's chuuni-name generation to OpenAI gpt-4o-mini.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:openai": "wrangler secret put OPENAI_API_KEY",
    "secret:app": "wrangler secret put APP_SECRET"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.60.0"
  }
}
